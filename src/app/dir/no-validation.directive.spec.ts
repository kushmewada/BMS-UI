import { NoValidationDirective } from './no-validation.directive';

describe('NoValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new NoValidationDirective();
    expect(directive).toBeTruthy();
  });
});
