import { PunchTimePipe } from './punch-time.pipe';

describe('PunchTimePipe', () => {
  it('create an instance', () => {
    const pipe = new PunchTimePipe();
    expect(pipe).toBeTruthy();
  });
});
